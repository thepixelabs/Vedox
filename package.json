{
  "name": "vedox",
  "version": "0.1.0",
  "private": true,
  "description": "Vedox — local-first, Git-native documentation CMS",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "turbo": "^2.3.3",
    "wait-on": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
