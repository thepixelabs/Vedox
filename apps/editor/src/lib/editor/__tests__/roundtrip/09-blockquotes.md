## Blockquotes

A simple blockquote:

> This is a blockquote. It preserves the visual hierarchy of the original document
> and is useful for callouts, notes, and excerpts.

A blockquote with attribution:

> "Obsidian for software project documentation — local-first, Git-native, with a real WYSIWYG and AI ingestion."
>
> — Vedox tagline

Nested blockquotes:

> Outer quote level one.
>
> > Nested quote level two.
> >
> > Still in the nested quote.
>
> Back to outer level.

A blockquote containing a code block:

> Example configuration:
>
> ```typescript
> export default {
>   workspace: './docs',
>   port: 3001
> };
> ```
>
> This is the minimal `vedox.config.ts`.
